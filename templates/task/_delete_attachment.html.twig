<form method="post" action="{{ path('task_attachment_delete', {'id': task.id}) }}" onsubmit="return confirm('Are you sure you want to delete attachment?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete-attachment' ~ task.id) }}">
    <button class="btn btn-link">Delete</button>
</form>
